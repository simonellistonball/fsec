import { OutputKeyValuesPipe } from './output-key-values.pipe';

describe('OutputKeyValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new OutputKeyValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
