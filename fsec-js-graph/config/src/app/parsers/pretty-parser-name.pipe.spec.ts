import { PrettyParserNamePipe } from './pretty-parser-name.pipe';

describe('PrettyParserNamePipe', () => {
  it('create an instance', () => {
    const pipe = new PrettyParserNamePipe();
    expect(pipe).toBeTruthy();
  });
});
